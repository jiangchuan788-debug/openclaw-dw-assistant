# AGENTS.md - 会话配置模板

**用途**: 定义 OpenClaw 会话的启动行为和规则
**使用方法**: 复制此文件为 `AGENTS.md` 并根据实际情况填写

---

## 会话启动规则

### 首次启动
当 `BOOTSTRAP.md` 存在时，按照其指引完成初始化，然后删除该文件。

### 每次会话启动
1. 读取 `SOUL.md` - 了解当前身份和个性
2. 读取 `USER.md` - 了解用户信息
3. 读取 `memory/YYYY-MM-DD.md` (今天和昨天) - 获取近期上下文
4. **如果是主会话** (直接对话): 同时读取 `MEMORY.md` 长期记忆

---

## 记忆管理规则

### 日常记录
- **每日笔记**: `memory/YYYY-MM-DD.md` - 记录当天发生的事件
- **长期记忆**: `MEMORY.md` - 整理后的重要信息

### 记忆维护
定期（每隔几天）检查并更新 MEMORY.md，将重要信息从每日笔记迁移到长期记忆。

---

## 行为准则

### 安全边界
- 不泄露私有数据
- 不执行破坏性命令（`rm` 等）
- 不确定时询问用户

### 工具使用
- 参考 `TOOLS.md` 中的工具配置
- 优先使用技能（Skills）完成复杂任务

---

## 会话类型识别

| 类型 | 说明 | 是否读取 MEMORY.md |
|------|------|-------------------|
| 主会话 | 直接对话 | ✅ 读取 |
| 群聊 | 群组中的对话 | ❌ 不读取 |
| 子代理 | 派生的子会话 | ❌ 不读取 |

---

**注意**: 此文件为模板，请复制后根据实际情况修改。
